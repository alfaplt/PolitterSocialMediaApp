<script>

    $(function () {
        $("body").on("click", ".followers", function () {
            var url = $(this).data("target");
            $.post(url, function (data) { })

                .done(function (data) {
                    $("#modelViewforFollows .modal-body").html(data);
                    $("#modelViewforFollows #myModalLabel").text("Takipçiler");
                    $("#modelViewforFollows").modal("show");
                })

                .fail(function () {
                    $("#modelViewforFollows .modal-body").text("Opps!!");
                    $("#modelViewforFollows").modal("show");
                })

        });
    })

    $(function () {
        $("body").on("click", ".followings", function () {
            var url = $(this).data("target");
            $.post(url, function (data) { })

                .done(function (data) {
                    $("#modelViewforFollows .modal-body").html(data);
                    $("#modelViewforFollows #myModalLabel").text("Takip Edilenler");
                    $("#modelViewforFollows").modal("show");
                })

                .fail(function () {
                    $("#modelViewforFollows .modal-body").text("Opps!!");
                    $("#modelViewforFollows").modal("show");
                })

        });
    })

</script>

<div class="modal fade" id="modelViewforFollows">
    <div class="modal-dialog modal-dialog-scrollable modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <div class="modal-title h4" id="myModalLabel"></div>
                <button type="button" class="close " data-bs-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                @*_ListComments PartialView*@
            </div>
        </div>
    </div>
</div>
